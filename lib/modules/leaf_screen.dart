
import 'package:flutter/material.dart';

class LeafScreen extends  StatelessWidget {
  const LeafScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('LeafScreen');
  }
}
